#!/usr/bin/env bash
# Deploy all (Algolia + images)
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Deploying to Algolia..."
node "$SCRIPT_DIR/algolia.js"

echo ""
echo "Deploying images to CDN..."
bash "$SCRIPT_DIR/images"

echo ""
echo "Deployment complete"
